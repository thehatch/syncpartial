<div>
    <h1><%=title%></h1>
    <p><%=text%></p>
</div>
<div class="comments">
    <% if(typeof comments != "undefined" && comments) { %>
        <% for(var comment in comments) { %>
            <%- syncpartial('comment.bliss', comments[comment], "page123") %>
        <% } %>
    <% } %>
</div>